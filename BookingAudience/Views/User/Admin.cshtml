<div class="category-content admin">
    <div class="category-title">@ViewData["Title"]</div>
    <div class="view-page">

        @Html.PartialAsync("Adminb");
        @Html.PartialAsync("Admina");
        @Html.PartialAsync("Adminu");

    </div>
</div>